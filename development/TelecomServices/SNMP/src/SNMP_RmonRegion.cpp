/*-----------------------------------------------------------------------------
   Copyright(c) Tellabs Transport Group Inc. All rights reserved.

SUBSYSTEM:      
TARGET:         
AUTHOR:         
DESCRIPTION:    
-----------------------------------------------------------------------------*/ 

/*
#include <SNMP/SNMP_RmonRegion.h>

SNMP_BbRmonEtherHistData& SNMP_RmonEtherHistDataRegion::GetHistoryData(CT_SNMP_Integer32 theIndex)
    throw (FC_RuntimeError)
{
    int index;
    for (index = 0; index < this->Size(); index++) 
    {
        if ((*this)[index].GetIndex() == theIndex) 
        {
            break;
        }
    }

    if (index == this->Size()) 
    {
        FC_THROW_ERROR(FC_RuntimeError, "RMON Ethernet history data not found");
    }
    else
    {
        return ((*this)[index])
    }
}
*/
